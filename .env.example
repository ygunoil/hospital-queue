# 复制为 backend/.env 或项目根目录 .env（后端从 backend 目录加载）

# LLM（OpenAI 兼容，完全可配置；留空或 LLM_USE_MOCK=true 则走规则 Mock）
LLM_MODEL=mimo-v2.5
LLM_API_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
LLM_API_KEY=
LLM_USE_MOCK=false

# 诊室信息
DEPARTMENT=心内科
ROOM=3号诊室
DOCTOR_NAME=赵主任

# 服务端口
HOST=0.0.0.0
PORT=8000

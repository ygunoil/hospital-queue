{
  "name": "hospital-queue-system",
  "private": true,
  "scripts": {
    "start": "bash scripts/start.sh",
    "backend": "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "frontend": "cd frontend && pnpm run dev"
  }
}
